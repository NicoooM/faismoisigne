
  <div class="users">
  <% @users.each do |user| %>
  <div class="card col-sm-4">
  <div class="row">
  <div class="card-body">
    <%= user.prenom %> <%= user.nom%><br>
    <%= user.description %><br>
    <%= link_to 'Voir le profil', profile_path(user.id)%>
  </div>
  </div> 
  
  </div>
  <% end %>
  </div>

