  <div class="container">
    <h1>Découvrez tous nos événements</h1>
    <div class="d-flex justify-content-center mb-4">
      <%= link_to '+ Créer un évènement', new_event_path(@events), class: 'btn btn-primary' %>
    </div>
    <div class="news_corona">
    <h4 style="text-align: center; color:red;" > En raison de l'actualité, il se peut que certains événements soient perturbés. <br> Contactez l'organisateur pour en savoir plus.</h4>
    <center><a href="https://www.gouvernement.fr/" style="color:red;"> Consultez les dernières informations sur l’épidémie de Covid-19 en France</a>
  </div> 
    <% if @future_events.any? %>
      <h2 class="mt-5">Événements à venir</h2>
      <div class="row">
        <% @future_events.each do |event| %>
          <%= render event %>
        <% end %>
      </div>
    <% end %>
    <h2 class="mt-5">Événements passés</h2>
    <div class="row">
      <% @past_events.each do |event| %>
        <%= render event %>
      <% end %>
    </div>
  </div>
</div>
