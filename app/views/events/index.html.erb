<head>
<link href="//stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
</head>  
 <div class="events">

    <h2 class="title">Rencontres</h2>
    <div class="container">
      <div class="banniere">     
        <%= image_tag 'banlong.png', class: "", :width => "100%"; %>
      </div> 
    <div class="row">
      <% @events.each do |event| %>
      <div class="col-sm-4"> 
        <div class="card"> 
            <div class="card-body">
              <h3 class="card-title"><%= event.nom %></h3>
              <p class="card-text">
              <p class="user"><strong>Organisateur </strong><p><%= link_to event.user_contact, profile_path(event.user_id), :class => "link"%></p>
                
              <p class="date">Le <%= event.date.strftime('%d/%m/%Y')%> à <%= event.heure.strftime('%H:%M') %></p> 
              <p class="lieu"><%= link_to event.lieu, 'https://www.google.fr/maps/search/' +  event.lieu + '+' + event.num_rue.to_s + '+' + event.rue + '+' + event.code_postal.to_s + '+' +  event.ville , target: "_blank", :class => "link"%> à <%= event.ville %></p> 
              <div class="bottom">
              <%= link_to 'Afficher', event, :class => "btn btn-outline-primary btn-sm"%>
              <% if user_signed_in? && event.user_id == current_user.id %>
              <%= link_to 'Modifier', edit_event_path(event), :class => "btn btn-outline-secondary btn-sm" %>
              </i><%= link_to 'Supprimer', event, :class => "btn btn-outline-danger btn-sm", method: :delete, data: { confirm: 'Are you sure?' } %>
              <% end %> 
            </div> 
            </div>  
        </div>
      </div>
      <% end %>
    </div>
  </div>
</div>
